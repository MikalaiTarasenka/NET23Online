@model List<AnimalWorld.Web.Models.Animals.AnimalSpeciesViewModel>
@{
    ViewData["Title"] = "Список видов";
}

@section Styles {
    <link rel="stylesheet" href="~/css/common/admin.css" />
}

<section class="section">
    <div class="admin-header">
        <h2 class="admin-title">Список видов</h2>
    </div>

    <a href="/Home/Moderating" class="admin-back">← Назад к панели управления</a>

    @if (Model != null && Model.Any())
    {
        <div class="admin-list">
            @foreach (var species in Model)
            {
                <div class="admin-list-item">
                    <span class="admin-list-name">@species.Name</span>
                    <div class="admin-list-actions">
                        <a href="/AnimalSpecies/Form?id=@species.Id" class="btn btn-secondary">Изменить</a>
                        <form method="post" asp-controller="AnimalSpecies" asp-action="Delete" style="display: inline;">
                            <input type="hidden" name="id" value="@species.Id" />
                            <button type="submit" class="btn btn-danger" onclick="return confirm('Вы уверены?')">Удалить</button>
                        </form>
                    </div>
                </div>
            }
        </div>
    }
    else
    {
        <div class="admin-empty">
            <p>Виды животных не найдены.</p>
            <a href="/AnimalSpecies/Form" class="btn btn-primary">Создать вид</a>
        </div>
    }
</section>