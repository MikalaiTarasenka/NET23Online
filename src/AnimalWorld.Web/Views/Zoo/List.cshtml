@model List<AnimalWorld.Web.Models.Zoos.ZooViewModel>
@{
    ViewData["Title"] = "Список зоопарков";
}

@section Styles {
    <link rel="stylesheet" href="~/css/common/admin.css" />
}

<section class="section">
    <div class="admin-header">
        <h2 class="admin-title">Список зоопарков</h2>
    </div>

    <a href="/Home/Moderating" class="admin-back">← Назад к панели управления</a>

    @if (Model != null && Model.Any())
    {
        <div class="admin-list">
            @foreach (var zoo in Model)
            {
                <div class="admin-list-item">
                    <span class="admin-list-name">@zoo.Name</span>
                    <div class="admin-list-actions">
                        <a href="/Zoo/Form?id=@zoo.Id" class="btn btn-secondary">Изменить</a>
                        <a href="/Zoo/Binding?id=@zoo.Id" class="btn btn-secondary">Животные</a>
                        <form method="post" asp-controller="Zoo" asp-action="Delete" style="display: inline;">
                            <input type="hidden" name="id" value="@zoo.Id" />
                            <button type="submit" class="btn btn-danger" onclick="return confirm('Вы уверены?')">Удалить</button>
                        </form>
                    </div>
                </div>
            }
        </div>
    }
    else
    {
        <div class="admin-empty">
            <p>Зоопарки не найдены.</p>
            <a href="/Zoo/Form" class="btn btn-primary">Создать зоопарк</a>
        </div>
    }
</section>