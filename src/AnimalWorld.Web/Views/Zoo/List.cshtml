@model List<AnimalWorld.Web.Models.Zoos.ZooViewModel>

@{
    ViewData["Title"] = "Edit Zoos";
}

<div>
    <h2>Zoos</h2>
    <table style="width: 100%; border-collapse: collapse;">
        <thead>
            <tr style="background-color: #f0f0f0;">
                <th style="padding: 10px; text-align: left; border: 1px solid #ddd;">Name</th>
                <th style="padding: 10px; text-align: center; border: 1px solid #ddd;">Actions</th>
            </tr>
        </thead>
        <tbody>
            @if (Model != null && Model.Any())
            {
                foreach (var zoo in Model)
                {
                    <tr>
                        <td style="padding: 10px; border: 1px solid #ddd;">@zoo.Name</td>
                        <td style="padding: 10px; text-align: center; border: 1px solid #ddd;">
                            <a href="/Zoo/Form?Id=@zoo.Id" style="margin-right: 10px; color: #2980b9;">Изменить</a>
                            <form method="post" asp-controller="Zoo" asp-action="Delete" style="display: inline;">
                                <input type="hidden" name="Id" value="@zoo.Id" />
                                <button type="submit" style="color: #c0392b; background: none; border: none; cursor: pointer;" onclick="return confirm('Are you sure?')">Удалить</button>
                            </form>
                            <a href="/Zoo/Binding?Id=@zoo.Id" style="margin-right: 10px; color: #2980b9;">Редактировать список животных</a>
                        </td>
                    </tr>
                }
            }
            else
            {
                <tr>
                    <td colspan="2" style="padding: 10px; text-align: center; border: 1px solid #ddd;">No zoos found.</td>
                </tr>
            }
        </tbody>
    </table>
</div>